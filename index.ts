const recentTT: HTMLDivElement = document.querySelector(".recent-tt")!;
const recentPH: HTMLDivElement = document.querySelector(".recent-ph")!;

// TODO: Use this lines to show and hide placeholder
recentPH.classList.toggle("show", false);
recentTT.classList.toggle("show", true);

// recentPH.classList.toggle("show", true);
// recentTT.classList.toggle("show", false);

//
